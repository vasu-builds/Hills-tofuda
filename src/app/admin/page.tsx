import AdminDashboard from '@/components/sections/AdminDashboard'

export const metadata = {
  title: 'Admin — Hills Tofuda',
  robots: { index: false, follow: false },
}

export default function Admin() {
  return <AdminDashboard />
}
